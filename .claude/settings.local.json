{
  "permissions": {
    "allow": [
      "Bash(python test_coords.py)",
      "Bash(python -c \"\nwith open(''templates/index.html'', ''r'', encoding=''utf-8'') as f:\n    content = f.read()\n\n# Reemplazar editando por datos en los valores de los inputs de la tabla\ncontent = content.replace(''editando.terreno_m2 if editando'', ''datos.terreno_m2 if datos'')\ncontent = content.replace(''editando.terreno_frente_lado if editando'', ''datos.terreno_frente_lado if datos'')\ncontent = content.replace(''editando.terreno_antes_revision if editando'', ''datos.terreno_antes_revision if datos'')\ncontent = content.replace(''editando.usd_m2_terreno if editando'', ''datos.usd_m2_terreno if datos'')\ncontent = content.replace(''editando.sup_edif_m2 if editando'', ''datos.sup_edif_m2 if datos'')\ncontent = content.replace(''editando.edif_frente_lado if editando'', ''datos.edif_frente_lado if datos'')\ncontent = content.replace(''editando.edif_antes_revision if editando'', ''datos.edif_antes_revision if datos'')\ncontent = content.replace(''editando.usd_m2_edif if editando'', ''datos.usd_m2_edif if datos'')\ncontent = content.replace(''editando.valor_dolar if editando'', ''datos.valor_dolar if datos'')\ncontent = content.replace(''editando.propuesta if editando'', ''datos.propuesta if datos'')\ncontent = content.replace(''editando.gmaps_zona if editando'', ''datos.gmaps_zona if datos'')\ncontent = content.replace(''editando.gmaps_frente if editando'', ''datos.gmaps_frente if datos'')\ncontent = content.replace(''editando.terreno_total if editando'', ''datos.terreno_total if datos'')\ncontent = content.replace(''editando.fot if editando'', ''datos.fot if datos'')\ncontent = content.replace(''editando.fos if editando'', ''datos.fos if datos'')\ncontent = content.replace(''editando.sup_edif_total if editando'', ''datos.sup_edif_total if datos'')\ncontent = content.replace(''editando.pisos_maximos if editando'', ''datos.pisos_maximos if datos'')\ncontent = content.replace(''editando.observaciones if editando'', ''datos.observaciones if datos'')\n\nwith open(''templates/index.html'', ''w'', encoding=''utf-8'') as f:\n    f.write(content)\n\nprint(''Reemplazos completados'')\n\")"
    ],
    "deny": [],
    "ask": []
  }
}
